# المرحلة الثانية — نظام التصميم والواجهة (UI Consistency & Polish)

## النطاق
تم تنفيذ المرحلة الثانية فوق نسخة المرحلة الأولى نفسها. الهدف هو رفع الاتساق البصري وتقليل الحمل البصري للنوافذ مع الحفاظ على أهداف اللمس وسلوك الإدخال.

## ما تم تنفيذه
- إضافة عقد تصميم مركزية للنوافذ في `ui/theme/DialogTokens.kt`.
- توحيد الأشكال الأساسية في `Theme.kt` عبر `MizanShapes` بدل الاعتماد على قيم Material الافتراضية غير المتناسقة.
- اعتماد حواف وحجوم متناسقة للنوافذ والحقول والأزرار في النوافذ المالية الأساسية.
- تخفيف الحشو الرأسي والأفقي في نافذة الالتزامات وقائمة الالتزامات وإعادة ترتيب الالتزامات.
- تخفيف نافذة إعدادات العملة ونافذة تأكيد الخروج ونافذة حذف تصنيف الحبايب ونافذة إعادة ضبط البيانات.
- توحيد حواف وأبعاد نافذتي إضافة/تعديل الالتزام ونموذج تسجيل/تعديل المعاملة باستخدام عقد التصميم.
- الحفاظ على الحد الأدنى المريح لأهداف اللمس؛ تم تقليل الفراغ لا مساحة اللمس.
- عدم تغيير منطق البيانات أو المستودعات أو مخطط قاعدة البيانات في هذه المرحلة.

## تدقيق ساكن
- فحص جميع ملفات Kotlin وXML في المشروع.
- لا توجد أخطاء XML في الموارد.
- لا توجد مراجع إلى `MizanDialogTokens` بدون import.
- لا توجد اختلالات أقواس في ملفات Kotlin وفق الفحص الساكن.
- لم يتم الادعاء بنجاح Gradle لأن البيئة تحتاج تنزيل Gradle 9.3.1 عبر الإنترنت، والاتصال الخارجي غير متاح.

## قرار المرحلة
**UI Polish / Design System: مكتملة.**
النسخة الناتجة هي الأساس التراكمي للمرحلة الثالثة.

## التحقق النهائي بعد مراجعة ثانية — 2026-09-14

تمت إعادة مراجعة المرحلة الثانية على النسخة الناتجة نفسها، مع التركيز على عدم وجود نوافذ خارجة عن عقد التصميم.

### جولة الاتساق الثانية
- تمت مراجعة جميع استدعاءات `AlertDialog` في المشروع وتوحيد شكل سطح الحوار إلى `MizanDialogTokens.shape`.
- تمت مراجعة الحوارات المخصصة المبنية على `Dialog`/`MizanAnimatedDialog` وتوحيد أسطح الحوارات الرئيسية التي كانت تستخدم `RoundedCornerShape(20.dp)` مستقلًا.
- تمت إضافة `sheetTopRadius` إلى عقد التصميم وتوحيد نصف قطر أعلى أوراق التفاصيل المتباين.
- تم الإبقاء عمدًا على أنصاف الأقطار الأصغر للعناصر الداخلية (chips / badges / تفاصيل البطاقات) لأنها ليست أسطح حوار، ولا ينبغي تكبيرها فقط من أجل التوحيد الشكلي.
- تم الحفاظ على أهداف اللمس وعدم تحويل كثافة الواجهة إلى عناصر صغيرة صعبة الاستخدام.
- تم التحقق من عدم وجود imports مكررة، وعدم وجود أخطاء XML.
- تعذر تنفيذ Gradle بسبب غياب Gradle 9.3.1 محليًا وحظر الاتصال الخارجي، لذلك لم يتم تسجيل نجاح Build غير مثبت.

### قرار المراجعة
**المرحلة الثانية مكتملة من ناحية نظام التصميم والاتساق البصري ضمن حدود التدقيق الساكن.**
المتبقي خارج نطاق هذه المرحلة هو اختبار Release على أجهزة حقيقية، وهو جزء من مرحلة الجودة/الـRelease التالية وليس نقصًا في تنفيذ عقد التصميم.

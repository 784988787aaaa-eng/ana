# تنفيذ تدقيق الترخيص والنسخ الاحتياطي

تم تطبيق الإصلاحات التالية على مشروع الدفتر الذكي:

- أصبح عداد التجربة مبنياً على البيانات الحية في Room، مع احتساب العملاء والمعاملات الحية وعناصر السلة، واحتساب `habayeb_bundle` كعميل + عدد معاملاته الموجودة داخل JSON.
- نقل العنصر إلى السلة لا يحرر أي مقعد؛ الحذف النهائي فقط يحرر المقعد.
- بوابة الحفظ أصبحت ذرية على مستوى عملية الإنشاء عبر `Mutex`، وتتحقق من العدد الفعلي قبل الإدراج. إنشاء عميل مع قيد افتتاحي يحجز مقعدين، وتعديل المعاملة لا يستهلك مقعداً.
- المعاملات الدورية تخضع لنفس البوابة ولا تُنفذ جزئياً إذا لم يتوفر العدد الكافي من المقاعد.
- نافذة الترخيص تقرأ عداد العمليات من تدفق قاعدة البيانات الحي بدلاً من الاعتماد على قيمة محفوظة كمصدر للحقيقة.
- اسم النسخ الحالي أصبح `SNA_YYYY-MM-DD_HH-mm.sna`، مع إبقاء `SMN_*.slb` للاستيراد القديم فقط.
- النسخ المحلي ما زال مشفراً عبر محرك النسخ الحالي AES/GCM وPBKDF2-HMAC-SHA256.
- تم تفعيل مسار `serverAuthCode` إلى Worker: Android يرسل رمز التفويض أحادي الاستخدام، والـWorker يستبدله ويخزن Refresh Token مشفراً في KV، ولا يصل Refresh Token إلى APK.
- عمليات Drive عند وجود جلسة Worker تستخدم `/driveApi/*`، مع الإبقاء على المسار المباشر كخلفية توافقية للحسابات القديمة.
- جلسة Worker تُجدد مدة صلاحيتها عند الاستخدام، بينما يبقى Refresh Token مشفراً على الخادم.
- إشعار النسخ الليلي يستخدم قناة `النسخ الاحتياطي للدفتر الذكي` وأيقونة `ic_splash_logo`، ويعرض اسم ملف `SNA_...` الحقيقي.
- الاهتزاز المزدوج `40ms + 30ms + 40ms` مخصص للنسخ اليدوي فقط؛ لا يوجد اهتزاز من Worker الليلي.
- Snackbar النسخ اليدوي يغلق تلقائياً بعد 2000ms ويعرض اسم الأرشيف.
- تم تحديث أسماء وامتدادات واجهة النسخ من `.slb` إلى `.sna`.

## ملاحظة عن Client ID

لم يتم اختراع أو تثبيت Client ID حرفي داخل المستودع. التطبيق يقرأ `GOOGLE_CLIENT_ID` من Gradle property أو متغير البيئة، كما يستطيع جلبه من `/driveApi/config` عند فتح واجهة النسخ. يجب أن يحتوي Cloudflare Worker في الإنتاج على `GOOGLE_CLIENT_ID` و`GOOGLE_CLIENT_SECRET` الحقيقيين من Google Cloud Console.

## التحقق

- تم فحص JavaScript للـWorker بواسطة `node --check` بنجاح.
- تم فحص ملفات XML المعدلة بنجاح.
- تعذر تشغيل Gradle/Kotlin compile في بيئة التنفيذ لأن Gradle Wrapper حاول تنزيل Gradle 9.3.1 من الإنترنت، والوصول الخارجي غير متاح هنا. لذلك لم أدّعِ نجاح بناء APK.

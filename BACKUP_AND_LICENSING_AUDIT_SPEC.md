# BACKUP_AND_LICENSING_AUDIT_SPEC.md

> **المشروع:** الدفتر الذكي / ميزان الدار
>
> **الدور التنفيذي:** Lead Principal Android System & Cloud Architect
>
> **نطاق المهمة:** تدقيق شامل ثم تنفيذ مضبوط لمنظومة النسخ الاحتياطي المحلي، التخزين المنظم، الاستعادة، Google Drive/OAuth 2.0، المزامنة وكشف التغيير، إدارة الجلسات، تراخيص Pro، حدود الأجهزة، Firebase Firestore، وواجهات النسخ/الترخيص.
>
> **قاعدة عليا:** لا يُسمح بأي تغيير يسبب فقدان بيانات، أو كسر ملفات `.mzd` الحالية، أو تغيير مفاتيح JSON القديمة، أو تغيير عقود التشفير دون مسار توافق صريح ومثبت بالاختبارات.

---

## 0. Executive Control Charter

هذه الوثيقة هي مواصفة التدقيق والتنفيذ المرجعية. لا تُنفذ تغييرات خارج ما هو محدد هنا إلا إذا أضيفت أولاً كملاحظة/قرار معماري في سجل التغيير ثم اعتمدت ضمن نفس الملف.

### القيود غير القابلة للتفاوض

1. **Zero Data Loss:** لا حذف لنسخة ناجحة قبل ضمان وجود نسخة بديلة مكتملة وقابلة للقراءة والتحقق.
2. **Backward Compatibility:** كل ملف `.mzd` سابق يجب أن يبقى قابلاً للقراءة والاستعادة؛ لا حذف أو إعادة تسمية أو تغيير لمعاني مفاتيح JSON الحالية.
3. **Atomic Commit:** لا تُعتبر عملية النسخ ناجحة قبل اكتمال الكتابة، مزامنة المحتوى إلى وسيط التخزين قدر الإمكان، إعادة التحقق، ثم إتمام الانتقال إلى الاسم النهائي.
4. **Integrity Before Trust:** لا يسمح للاستعادة أو الرفع أو إعلان النجاح بالاعتماد على `exists()` و`length > 0` فقط؛ يجب عبور فحص البنية والسلامة والبصمة المطلوبة.
5. **Single Source of Truth:** المسار الفعلي للنسخ المحلي وآلية بناء أسماء الملفات يجب أن تكون مركزية، ولا يسمح بإعادة تعريفها من مكونات أخرى.
6. **No Credential Leakage:** لا تسجيل لـ Access Token أو Refresh Token أو Authorization Header أو client secret أو محتوى `.mzd`.
7. **No Silent Destructive Recovery:** لا تُستخدم آلية إصلاح أو مزامنة تؤدي إلى استبدال نسخة أحدث بنسخة أقدم دون إثبات الفارق واتخاذ قرار صريح.
8. **No Random Refactor:** أي تنظيف برمجي يجب أن يحافظ على السلوك المقصود والعقود العامة والدوال التي تعتمد عليها أجزاء أخرى، أو يوفر طبقة توافق.
9. **Tracking is mandatory:** كل بند ينفذ ينتقل فوراً من `[ ]` إلى `[x]` في ملف التتبع التنفيذي، مع تسجيل الملف والدالة والنتيجة والاختبار.
10. **One logical change at a time:** لا تجمع عدة تغييرات حساسة في دفعة واحدة دون نقطة تحقق قابلة للاختبار.

---

# 1. Implementation & Tracking Protocol

## 1.1 ملف التتبع التنفيذي الإلزامي

`BACKUP_AND_LICENSING_AUDIT_TRACKER.md`

## 1.2 ترتيب التنفيذ الإلزامي

1. تثبيت baseline وتوثيق السلوك الحالي.
2. حفظ عقود التوافق القديمة.
3. توحيد مسار الملفات والمنطق المشترك.
4. إغلاق مخاطر الكتابة الذرية/سلامة الملف.
5. تقوية الاستعادة والتوافق مع الإصدارات السابقة.
6. تقوية Google OAuth والجلسات.
7. تثبيت Zero-Diff مع تحقق من حالة السحابة وليس الكاش المحلي فقط.
8. تقوية تراخيص الجهاز/Firebase/FIFO/offline.
9. تحسين واجهات النسخ والترخيص والحالات الطرفية.
10. اختبارات الاستمرارية، الانقطاع، إعادة التثبيت، وتلف الملفات.
11. مراجعة نهائية للـ Definition of Done.

# الدفتر الذكي — الإصدار التراكمي — Phase 3 High Integrity Test Plan

هذه المرحلة مبنية فوق آخر نسخة موجودة في الحزمة السابقة، ولا تستبدل اختبارات لوحة المفاتيح أو الاختبارات المالية الموجودة. كل مرحلة لاحقة يجب أن تُضاف إلى نفس خط الأساس التراكمي.

## نطاق الاختبارات

1. فتح وإغلاق النوافذ.
2. سلاسة الانتقال وعدم ظهور نافذة نصف مرسومة.
3. سرعة عرض البيانات عند بدء التطبيق ومنع الوميض المرئي.
4. دورة حياة نافذة الترحيب: مرة واحدة بعد التثبيت فقط.
5. النسخ الاحتياطي اليومي المحلي + السحابي.
6. بوابة الحد المجاني وعدّاد العمليات.
7. انتقال العملية بين الواجهة والسلة دون تكرار العد.
8. الحسابات التي تنتج معاملة واحدة.
9. الاستعادة الجزئية والكاملة من السلة.
10. التزامن والعمليات المتنافسة عند بلوغ الحد.

## تعريف "معاملة واحدة"

إذا أدخل المستخدم عملية حسابية مثل `1000 + 250 - 50` ثم حُفظ الناتج كسجل معاملة واحد، فهي **عملية واحدة** في عداد التجربة، بغض النظر عن عدد العمليات الحسابية الداخلية.

القاعدة ليست عدد أزرار الآلة الحاسبة أو عدد حدود التعبير، بل عدد السجلات المنطقية التي تم إنشاؤها في نموذج البيانات.

## عداد السلة

العداد يمثل **الوجود المنطقي للعملية** وليس عدد مرات ظهورها في واجهات التطبيق:

- سجل نشط: 1.
- نقله للسلة: يبقى 1.
- استعادته: يبقى 1.
- حذفه نهائياً: يصبح 0.

وبالنسبة لحزمة العميل:

- العميل = 1.
- كل معاملة داخل الحزمة = 1.
- الاستعادة الجزئية تنقل السجل من السلة إلى البيانات النشطة ولا تغيّر الإجمالي.
- الاستعادة النهائية تزيل الحزمة من السلة، ويبقى مجموع العميل ومعاملاته كما كان.

## اختبارات تجاوز الحد

عند `used = 100` يجب أن يفشل **أول إدخال جديد** قبل الإدراج، وأن يصدر حدث فتح نافذة الترخيص فوراً. لا يجوز الاعتماد على تحديث واجهة العداد بعد الإدخال لاكتشاف التجاوز.

عند `used = 99`:

- إنشاء معاملة واحدة = نجاح، يصبح الاستخدام 100.
- إنشاء معاملة ثانية = رفض ذري.

عند إنشاء عميل مع قيد افتتاحي:

- الحجز = مقعد العميل + مقعد المعاملة الافتتاحية = 2.
- إذا توفر مقعد واحد فقط، لا يُنشأ أي جزء من العملية.

## النسخ اليومي

المسار المقبول:

`جدولة فريدة → إنشاء نسخة محلية مشفرة → تأكيد الحفظ → رفع نفس الملف للسحابة عند توفر الربط`.

فشل السحابة لا يجب أن يمحو أو يبطل النسخة المحلية. فشل الشبكة المؤقت يجب أن يسمح بإعادة المحاولة بواسطة WorkManager.

تشغيل المهمة مرتين بالتوازي يجب ألا ينشئ نسختين متنافستين لنفس دورة النسخ.

## الترحيب

الترحيب جزء من **دورة تثبيت التطبيق** وليس من بيانات المستخدم القابلة للاستعادة. لذلك:

- لا يُعاد بسبب recomposition.
- لا يُعاد بسبب rotation.
- لا يُعاد بعد تحديث APK.
- لا يُعاد بسبب restore.
- لا يُعاد بسبب فتح التطبيق عشرات المرات.

عند إغلاقه تُحفظ `isFirstLaunch=false` و`onboardingShown=true` في كتابة إعدادات واحدة لمنع race condition.

## Startup / Flicker

SplashScreen تبقى حتى تحميل الإعدادات. هذا يمنع عرض الإعدادات الافتراضية المؤقتة قبل البيانات الحقيقية.

الاختبارات الآلية تقيس العقدة المعمارية، أما إثبات عدم وجود وميض بصري فعلي فيتطلب instrumentation/benchmark على جهاز؛ لذلك لا يُكتب "PASS لكل الهواتف" من اختبارات unit وحدها.

## نافذة/حركة

الحركة القياسية الحالية للنوافذ:

- دخول: 160ms.
- خروج: 120ms.
- Scale دخول: 0.98 → 1.0.
- Scale خروج: 1.0 → 0.98.

يجب أن تمر جميع مسارات الإغلاق عبر نفس cleanup للتركيز ولوحة المفاتيح قبل إزالة النافذة.

## بوابة الإصدار

أي فشل في:

- quota atomicity,
- double counting,
- onboarding persistence,
- local-first backup,
- unique daily scheduling,
- startup splash gating,
- window lifecycle,

يُعامل كـ release blocker حتى يتم فهم السبب وتصحيح الكود والاختبار معاً.

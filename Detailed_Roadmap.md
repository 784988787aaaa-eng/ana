# خريطة الطريق الفنية ومستند جاهزية المكونات (Detailed Roadmap & Component Status)

## خريطة حالة المكونات (Component Status Map)

| المكون / الملف | الحالة الحالية | الأثر البصري والوظيفي |
|---|---|---|
| `HabayebSortDropdownMenu.kt` | **READY** | قائمة فرز قائمة على هوية "ميزان" (Anchored Surface)، حواف 16dp، ظلال هادئة، مؤشر نشط خفيف (Active Pill + CheckIcon)، إزالة الفواصل المتكررة، وتوافق تام مع Dark Mode. |
| `CustomerHistoryTopBar.kt` | **READY** | ترويسة تفاصيل الحساب بكثافة بصرية متزنة، أهداف لمس قياسية ($\ge 44\text{-}48\text{dp}$) دون تضخيم الشريط، وأولوية مطلق لاسم العميل مع قطع مرن (Ellipsis) على الشاشات الضيقة. |
| `CustomerHistoryShareBottomSheet.kt` | **READY** | شيت المشاركة بتنسيق فاخر، ألوان شارات خفيفة ومتوافقة مع الثيم في الليل (Dark Mode Safe)، أزرار مشاركة PDF/CSV بأهداف لمس قياسية ($\ge 44\text{-}48\text{dp}$)، والتفريق البصري بين وثائق التقارير الشاملة والرسائل النصية السريعة. |
| `AddTransactionPopup.kt` | **READY** | ضبط حواف وهندسة نافذة إضافة المعاملة، أزرار بارتفاع 44dp ومساحات لمس $\ge 48\text{dp}$، وإدارة رشيقة للوحة المفاتيح عبر `FocusRequester` بدون تلاعب على مستوى النافذة. |
| `SearchLedgerDialog.kt` | **READY** | إلغاء `fillMaxSize` و`weight(1f)` عن الحالة الفارغة لتتبع النافذة محتواها، عرض أقصى 360dp، ارتفاع ديناميكي مدمج، تنظيف إدارة لوحة المفاتيح عبر FocusRequester والتخلص من الـ SOFT_INPUT القسري. |
| `SecurityScreen.kt` & `SecurityViewModel.kt` | **READY** | توفير مسار حفظ معلق ومتزامن `saveSettingsSuspend` مع معالجة الأخطاء، وضبط `SecurityDialog` بعرض أقصى 340dp، بادينغ 14dp، ومحاذاة RTL دقيقة مع إمكانية التمرير تحت الـ IME. |
| `SecuritySetupForm.kt` | **READY** | واجهة إدخال سلسة ومتماسكة لحفظ الرمز، انتقال تلقائي فوري بين الرمز والتأكيد والاسترداد، أيقونات إظهار/إخفاء هادئة 18-20dp، وبدون خلفيات بيضاء نافرة. |
| `AppLockScreen.kt` & `PasscodeKeypadContent.kt` | **READY** | اهتزاز خطأ الرمز هادئ ورصين (10f خلال 190ms بدون ارتداد طفولي)، تصغير تضخم القفل إلى 1.05f مع tween متزن، وتوزيع مريح للمسافات الرأسية بين الرأس والنقاط ولوحة الأرقام. |

---

## سجل التدقيق الفني للبصر والحركة (Visual & Interaction Audit Log)

### 1. قائمة الفرز الرئيسية (`HabayebSortDropdownMenu.kt`)
- **المشكلة السابقة:** كانت قائمة `DropdownMenu` تقليدية مع ألوان بيضاء ثابتة (`neutralWhite`) تفصل بينها فواصل أفقية متعددة، واعتمدت فقط على `FontWeight.Bold` لتمييز الخيار النشط.
- **التحسين المنفذ:** 
  - إعادة صياغة القائمة كـ Surface أنيق مخصص لـ "ميزان" بزوايا `16.dp` وحواف `outlineVariant.copy(alpha = 0.35f)`.
  - تمييز الخيار النشط بحاوية خفيفة جداً من لون الثيم `primary.copy(alpha = 0.10f)` وأيقونة صح صغيرة `Check` بنص semi-bold مقاس `13.sp`.
  - إزالة الفواصل الأفقية لتقليل الازدحام.
  - إغلاق القائمة فوراً والتأكد من استجابة اللمس القياسية $\ge 48\text{dp}$.

### 2. ترويسة تفاصيل الحساب (`CustomerHistoryTopBar.kt`)
- **المشكلة السابقة:** كانت أزرار التحكم مفروضة بحجم مرئي ومساحة لمس ثانوية `36.dp` مما أثر على سهولة النقر على الهواتف الصغيرة.
- **التحسين المنفذ:**
  - ضبط حاويات أزرار التحكم لتستوفي أهداف اللمس القياسية ($\ge 44\text{-}48\text{dp}$) باستخدام `defaultMinSize(minWidth = 44.dp, minHeight = 44.dp)` مع الحفاظ على الأيقونة البصرية بحجم `18-20.dp`.
  - إعطاء المساحة المرنة الكبرى لاسم العميل `maxLines = 1, overflow = TextOverflow.Ellipsis` لضمان عدم اختفائه أو انكسار السطر على الشاشات الضيقة (360dp).

### 3. نافذة المشاركة الحالية (`CustomerHistoryShareBottomSheet.kt`)
- **المشكلة السابقة:** استخدام ألوان خلفية فاتحة ثابتة للشارات (`DebtContainerLight`, `CreditContainerLight`) كانت تسبب تضارباً بصرياً ناصعاً في الوضع الليلي، وتزاحم أيقونات PDF/CSV بحجم 36dp.
- **التحسين المنفذ:**
  - استبدال الألوان الثابتة بتجميعات متوافقة مع الثيم `DebtRed.copy(alpha = 0.12f)` و `CreditGreen.copy(alpha = 0.12f)`.
  - توسيع أهداف اللمس لجميع أزرار المشاركة الثلاثة إلى $\ge 44\text{-}48\text{dp}$.
  - إضافة `verticalScroll` لحماية الشاشات ذات الارتفاع المنخفض من اقتطاع زر المشاركة السفلي.
  - توضيح التسلسل الهرمي بين التقرير الشامل (PDF / CSV) والرسائل النصية السريعة (WhatsApp / SMS).
